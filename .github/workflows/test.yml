name: テスト実行
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        id: setup_node_id
        with:
          node-version: 18
          cache: yarn

      # FIXME: キャッシュ周りが機能しなくなったので一旦無効化
      # - uses: actions/cache@v3
      #   id: node_modules_cache_id
      #   env:
      #     cache-name: cache-node-modules
      #   with:
      #     path: '**/node_modules'
      #     key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/yarn.lock') }}-v4

      # - run: echo '${{ toJSON(steps.setup_node_id.outputs) }}'
      # - run: echo '${{ steps.setup_node_id.outputs.cache-hit != 'true' }}'

      # - if: ${{ steps.setup_node_id.outputs.cache-hit != 'true' }}
      #   run: yarn install --frozen-lockfile

      - run: yarn install --frozen-lockfile

      - uses: ArtiomTr/jest-coverage-report-action@v2.0-rc.6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          package-manager: 'yarn'
          test-script: yarn run test:coverage
